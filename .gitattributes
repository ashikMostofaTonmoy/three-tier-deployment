# Shell scripts and configs run on Linux servers — force LF so they never
# break with "bad interpreter" or syntax errors after a checkout on Windows.
*.sh    text eol=lf
*.conf  text eol=lf
*.yml   text eol=lf
*.yaml  text eol=lf

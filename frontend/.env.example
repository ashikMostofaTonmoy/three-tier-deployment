# Optional. Only used at build time (npm run build).
# If set, this exact string is shown as the build version in the app footer.
# CI sets this automatically; you normally do NOT need this file for local dev.
BUILD_ID=local-test
